# Vector Store

This folder stores the local persisted Chroma vector database used by the app.

Generated database files are ignored from Git, but the folder remains visible in the repository so the project structure clearly shows vector database integration.
